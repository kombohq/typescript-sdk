/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { HTTPClient } from "./http.js";
import { Logger } from "./logger.js";
import { RetryConfig } from "./retries.js";
import { Params, pathToFunc } from "./url.js";

/**
 * Kombo EU Region
 */
export const ServerEu = "eu";
/**
 * Kombo US Region
 */
export const ServerUs = "us";
/**
 * Contains the list of servers available to the SDK
 */
export const ServerList = {
  [ServerEu]: "https://api.kombo.dev/v1",
  [ServerUs]: "https://api.us.kombo.dev/v1",
} as const;

export type SDKOptions = {
  api_key?: string | (() => Promise<string>) | undefined;

  /**
   * Allows setting the integration_id parameter for all supported operations
   */
  integration_id?: string | undefined;

  http_client?: HTTPClient;
  /**
   * Allows overriding the default server used by the SDK
   */
  server?: keyof typeof ServerList | undefined;
  /**
   * Allows overriding the default server URL used by the SDK
   */
  server_url?: string | undefined;
  /**
   * Allows overriding the default user agent used by the SDK
   */
  user_agent?: string | undefined;
  /**
   * Allows overriding the default retry config used by the SDK
   */
  retry_config?: RetryConfig;
  timeout_ms?: number;
  debug_logger?: Logger;
};

export function serverURLFromOptions(options: SDKOptions): URL | null {
  let serverURL = options.server_url;

  const params: Params = {};

  if (!serverURL) {
    const server = options.server ?? ServerEu;
    serverURL = ServerList[server] || "";
  }

  const u = pathToFunc(serverURL)(params);
  return new URL(u);
}

export const SDK_METADATA = {
  language: "typescript",
  openapiDocVersion: "1.0.0",
  sdkVersion: "0.2.1",
  genVersion: "2.753.1",
  userAgent: "speakeasy-sdk/typescript 0.2.1 2.753.1 1.0.0 @kombo-api/sdk",
} as const;
