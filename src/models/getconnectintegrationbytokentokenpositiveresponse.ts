/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { ClosedEnum } from "../types/enums.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * The setup_status is used in conjunction with the filtering and field mapping features. If these are enabled in the connection flow, the integration will start in an "INCOMPLETE" state and move to "COMPLETE" once all steps are finished.
 *
 * @remarks
 *
 * - `INCOMPLETE`: Setup is still in progress. Some steps aren’t finished, so no data is available yet. Syncs only run as needed for setup.
 * - `FINAL_SYNC_PENDING`: Setup is complete, and the final sync is running. Data will be available after this sync is done.
 * - `COMPLETED`: Setup is fully finished, and the integration is ready to use.
 */
export const GetConnectIntegrationByTokenTokenPositiveResponseSetupStatus = {
  Incomplete: "INCOMPLETE",
  FinalSyncPending: "FINAL_SYNC_PENDING",
  Completed: "COMPLETED",
} as const;
/**
 * The setup_status is used in conjunction with the filtering and field mapping features. If these are enabled in the connection flow, the integration will start in an "INCOMPLETE" state and move to "COMPLETE" once all steps are finished.
 *
 * @remarks
 *
 * - `INCOMPLETE`: Setup is still in progress. Some steps aren’t finished, so no data is available yet. Syncs only run as needed for setup.
 * - `FINAL_SYNC_PENDING`: Setup is complete, and the final sync is running. Data will be available after this sync is done.
 * - `COMPLETED`: Setup is fully finished, and the integration is ready to use.
 */
export type GetConnectIntegrationByTokenTokenPositiveResponseSetupStatus =
  ClosedEnum<
    typeof GetConnectIntegrationByTokenTokenPositiveResponseSetupStatus
  >;

export type GetConnectIntegrationByTokenTokenPositiveResponseData = {
  tool: string;
  id: string;
  end_user_origin_id: string | null;
  end_user_organization_name: string;
  end_user_email: string | null;
  /**
   * The setup_status is used in conjunction with the filtering and field mapping features. If these are enabled in the connection flow, the integration will start in an "INCOMPLETE" state and move to "COMPLETE" once all steps are finished.
   *
   * @remarks
   *
   * - `INCOMPLETE`: Setup is still in progress. Some steps aren’t finished, so no data is available yet. Syncs only run as needed for setup.
   * - `FINAL_SYNC_PENDING`: Setup is complete, and the final sync is running. Data will be available after this sync is done.
   * - `COMPLETED`: Setup is fully finished, and the integration is ready to use.
   */
  setup_status: GetConnectIntegrationByTokenTokenPositiveResponseSetupStatus;
};

export type GetConnectIntegrationByTokenTokenPositiveResponse = {
  status: "success";
  data: GetConnectIntegrationByTokenTokenPositiveResponseData;
};

/** @internal */
export const GetConnectIntegrationByTokenTokenPositiveResponseSetupStatus$inboundSchema:
  z.ZodNativeEnum<
    typeof GetConnectIntegrationByTokenTokenPositiveResponseSetupStatus
  > = z.nativeEnum(
    GetConnectIntegrationByTokenTokenPositiveResponseSetupStatus,
  );

/** @internal */
export const GetConnectIntegrationByTokenTokenPositiveResponseData$inboundSchema:
  z.ZodType<
    GetConnectIntegrationByTokenTokenPositiveResponseData,
    z.ZodTypeDef,
    unknown
  > = z.object({
    tool: z.string(),
    id: z.string(),
    end_user_origin_id: z.nullable(z.string()),
    end_user_organization_name: z.string(),
    end_user_email: z.nullable(z.string()),
    setup_status:
      GetConnectIntegrationByTokenTokenPositiveResponseSetupStatus$inboundSchema,
  });

export function getConnectIntegrationByTokenTokenPositiveResponseDataFromJSON(
  jsonString: string,
): SafeParseResult<
  GetConnectIntegrationByTokenTokenPositiveResponseData,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      GetConnectIntegrationByTokenTokenPositiveResponseData$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'GetConnectIntegrationByTokenTokenPositiveResponseData' from JSON`,
  );
}

/** @internal */
export const GetConnectIntegrationByTokenTokenPositiveResponse$inboundSchema:
  z.ZodType<
    GetConnectIntegrationByTokenTokenPositiveResponse,
    z.ZodTypeDef,
    unknown
  > = z.object({
    status: z.literal("success"),
    data: z.lazy(() =>
      GetConnectIntegrationByTokenTokenPositiveResponseData$inboundSchema
    ),
  });

export function getConnectIntegrationByTokenTokenPositiveResponseFromJSON(
  jsonString: string,
): SafeParseResult<
  GetConnectIntegrationByTokenTokenPositiveResponse,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      GetConnectIntegrationByTokenTokenPositiveResponse$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'GetConnectIntegrationByTokenTokenPositiveResponse' from JSON`,
  );
}
