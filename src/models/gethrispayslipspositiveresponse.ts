/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import * as z from "zod/v3";
import { safeParse } from "../lib/schemas.js";
import { Result as SafeParseResult } from "../types/fp.js";
import { RFCDate } from "../types/rfcdate.js";
import { SDKValidationError } from "./errors/sdkvalidationerror.js";

/**
 * The total gross pay for the payslip, before taxes and after gross deductions.
 */
export type GetHrisPayslipsPositiveResponseGrossPay = {
  /**
   * The [ISO 4217 currency code](https://www.iso.org/iso-4217-currency-codes.html) the value is denominated in.
   */
  currency: string;
  /**
   * The monetary value.
   */
  value: number;
};

/**
 * The total net pay for the payslip, after taxes have been applied to the gross pay.
 */
export type GetHrisPayslipsPositiveResponseNetPay = {
  /**
   * The [ISO 4217 currency code](https://www.iso.org/iso-4217-currency-codes.html) the value is denominated in.
   */
  currency: string;
  /**
   * The monetary value.
   */
  value: number;
};

/**
 * The amount of the payslip that was actually paid out to the employee. This value accounts for net earnings and deductions.
 */
export type GetHrisPayslipsPositiveResponsePaidAmount = {
  /**
   * The [ISO 4217 currency code](https://www.iso.org/iso-4217-currency-codes.html) the value is denominated in.
   */
  currency: string;
  /**
   * The monetary value.
   */
  value: number;
};

export type GetHrisPayslipsPositiveResponseTotals = {
  /**
   * The total gross pay for the payslip, before taxes and after gross deductions.
   */
  gross_pay?: GetHrisPayslipsPositiveResponseGrossPay | null | undefined;
  /**
   * The total net pay for the payslip, after taxes have been applied to the gross pay.
   */
  net_pay?: GetHrisPayslipsPositiveResponseNetPay | null | undefined;
  /**
   * The amount of the payslip that was actually paid out to the employee. This value accounts for net earnings and deductions.
   */
  paid_amount?: GetHrisPayslipsPositiveResponsePaidAmount | null | undefined;
};

/**
 * The employee that the payslip is for.
 */
export type Employee = {
  /**
   * The globally unique ID of this object generated by Kombo. We recommend using this as a stable primary key for syncing.
   */
  id: string;
  /**
   * The raw ID of the object in the remote system. We don't recommend using this as a primary key on your side as it might sometimes be compromised of multiple identifiers if a system doesn't provide a clear primary key.
   */
  remote_id: string;
  /**
   * The employee’s first name.
   */
  first_name: string | null;
  /**
   * The employee’s last name.
   */
  last_name: string | null;
  /**
   * The employee’s full name, including any middle names. Not all HR systems provide an explicit display name, so we recommend falling back to `first_name` and `last_name`.
   */
  display_full_name: string | null;
  /**
   * The employee’s work email address. If the email address is invalid, we will set this to `null`.
   */
  work_email?: string | null | undefined;
  /**
   * The date and time the object was deleted in the remote system. Objects are automatically marked as deleted when Kombo can't retrieve them from the remote system anymore. Kombo will also anonymize entries 14 days after they disappear.
   */
  remote_deleted_at: Date | null;
};

/**
 * The pay run the payslip was paid out through.
 */
export type PayRun = {
  /**
   * The globally unique ID of this object generated by Kombo. We recommend using this as a stable primary key for syncing.
   */
  id: string;
  /**
   * The raw ID of the object in the remote system. We don't recommend using this as a primary key on your side as it might sometimes be compromised of multiple identifiers if a system doesn't provide a clear primary key.
   */
  remote_id: string;
  /**
   * The start date of the pay run, as a calendar date (`yyyy-MM-dd`).
   */
  start_date: RFCDate | null;
  /**
   * The end date of the pay run, as a calendar date (`yyyy-MM-dd`).
   */
  end_date: RFCDate | null;
  /**
   * The Kombo ID of the pay run’s legal entity. The ID can be used to retrieve the legal entity from the `get legal entities` endpoint.
   */
  legal_entity_id: string | null;
};

/**
 * The amount of the line item.
 */
export type Amount = {
  /**
   * The [ISO 4217 currency code](https://www.iso.org/iso-4217-currency-codes.html) the value is denominated in.
   */
  currency: string;
  /**
   * The monetary value.
   */
  value: number;
};

export type LineItem = {
  /**
   * The globally unique ID of this object generated by Kombo. We recommend using this as a stable primary key for syncing. For some integrations, this ID can still change if the payslip is edited.
   */
  id: string;
  /**
   * The raw ID of the object in the remote system. We don't recommend using this as a primary key on your side as it might sometimes be compromised of multiple identifiers if a system doesn't provide a clear primary key. This value may include the item's position on the payslip, which can change if the payslip is edited.
   */
  remote_id: string;
  /**
   * The name of the salary type.
   */
  name: string | null;
  /**
   * The amount of the line item.
   */
  amount?: Amount | null | undefined;
};

export type GetHrisPayslipsPositiveResponseResult = {
  /**
   * The globally unique ID of this object generated by Kombo. We recommend using this as a stable primary key for syncing.
   */
  id: string;
  /**
   * The raw ID of the object in the remote system. We don't recommend using this as a primary key on your side as it might sometimes be compromised of multiple identifiers if a system doesn't provide a clear primary key.
   */
  remote_id: string;
  /**
   * The timestamp when this specific record was last modified. This field only updates when properties directly on this record change, NOT when related or nested models change. For filtering that considers nested data changes, use the `updated_after` parameter which will return records when either the record itself OR its related models have been updated.
   */
  changed_at: Date;
  /**
   * The date and time the object was deleted in the remote system. Objects are automatically marked as deleted when Kombo can't retrieve them from the remote system anymore. Kombo will also anonymize entries 14 days after they disappear.
   */
  remote_deleted_at: Date | null;
  totals: GetHrisPayslipsPositiveResponseTotals;
  /**
   * The employee that the payslip is for.
   */
  employee: Employee;
  /**
   * The pay run the payslip was paid out through.
   */
  pay_run: PayRun | null;
  /**
   * The individual earnings, deductions, and contributions that make up the payslip.
   */
  line_items: Array<LineItem>;
};

export type GetHrisPayslipsPositiveResponseData = {
  /**
   * Cursor string that can be passed to the `cursor` query parameter to get the next page. If this is `null`, then there are no more pages.
   */
  next: string | null;
  results: Array<GetHrisPayslipsPositiveResponseResult>;
};

export type GetHrisPayslipsPositiveResponse = {
  status: "success";
  data: GetHrisPayslipsPositiveResponseData;
};

/** @internal */
export const GetHrisPayslipsPositiveResponseGrossPay$inboundSchema: z.ZodType<
  GetHrisPayslipsPositiveResponseGrossPay,
  z.ZodTypeDef,
  unknown
> = z.object({
  currency: z.string(),
  value: z.number(),
});

export function getHrisPayslipsPositiveResponseGrossPayFromJSON(
  jsonString: string,
): SafeParseResult<
  GetHrisPayslipsPositiveResponseGrossPay,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      GetHrisPayslipsPositiveResponseGrossPay$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'GetHrisPayslipsPositiveResponseGrossPay' from JSON`,
  );
}

/** @internal */
export const GetHrisPayslipsPositiveResponseNetPay$inboundSchema: z.ZodType<
  GetHrisPayslipsPositiveResponseNetPay,
  z.ZodTypeDef,
  unknown
> = z.object({
  currency: z.string(),
  value: z.number(),
});

export function getHrisPayslipsPositiveResponseNetPayFromJSON(
  jsonString: string,
): SafeParseResult<GetHrisPayslipsPositiveResponseNetPay, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      GetHrisPayslipsPositiveResponseNetPay$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetHrisPayslipsPositiveResponseNetPay' from JSON`,
  );
}

/** @internal */
export const GetHrisPayslipsPositiveResponsePaidAmount$inboundSchema: z.ZodType<
  GetHrisPayslipsPositiveResponsePaidAmount,
  z.ZodTypeDef,
  unknown
> = z.object({
  currency: z.string(),
  value: z.number(),
});

export function getHrisPayslipsPositiveResponsePaidAmountFromJSON(
  jsonString: string,
): SafeParseResult<
  GetHrisPayslipsPositiveResponsePaidAmount,
  SDKValidationError
> {
  return safeParse(
    jsonString,
    (x) =>
      GetHrisPayslipsPositiveResponsePaidAmount$inboundSchema.parse(
        JSON.parse(x),
      ),
    `Failed to parse 'GetHrisPayslipsPositiveResponsePaidAmount' from JSON`,
  );
}

/** @internal */
export const GetHrisPayslipsPositiveResponseTotals$inboundSchema: z.ZodType<
  GetHrisPayslipsPositiveResponseTotals,
  z.ZodTypeDef,
  unknown
> = z.object({
  gross_pay: z.nullable(
    z.lazy(() => GetHrisPayslipsPositiveResponseGrossPay$inboundSchema),
  ).optional(),
  net_pay: z.nullable(
    z.lazy(() => GetHrisPayslipsPositiveResponseNetPay$inboundSchema),
  ).optional(),
  paid_amount: z.nullable(
    z.lazy(() => GetHrisPayslipsPositiveResponsePaidAmount$inboundSchema),
  ).optional(),
});

export function getHrisPayslipsPositiveResponseTotalsFromJSON(
  jsonString: string,
): SafeParseResult<GetHrisPayslipsPositiveResponseTotals, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      GetHrisPayslipsPositiveResponseTotals$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetHrisPayslipsPositiveResponseTotals' from JSON`,
  );
}

/** @internal */
export const Employee$inboundSchema: z.ZodType<
  Employee,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  remote_id: z.string(),
  first_name: z.nullable(z.string()),
  last_name: z.nullable(z.string()),
  display_full_name: z.nullable(z.string()),
  work_email: z.nullable(z.string()).optional(),
  remote_deleted_at: z.nullable(
    z.string().datetime({ offset: true }).transform(v => new Date(v)),
  ),
});

export function employeeFromJSON(
  jsonString: string,
): SafeParseResult<Employee, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Employee$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Employee' from JSON`,
  );
}

/** @internal */
export const PayRun$inboundSchema: z.ZodType<PayRun, z.ZodTypeDef, unknown> = z
  .object({
    id: z.string(),
    remote_id: z.string(),
    start_date: z.nullable(z.string().transform(v => new RFCDate(v))),
    end_date: z.nullable(z.string().transform(v => new RFCDate(v))),
    legal_entity_id: z.nullable(z.string()),
  });

export function payRunFromJSON(
  jsonString: string,
): SafeParseResult<PayRun, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => PayRun$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'PayRun' from JSON`,
  );
}

/** @internal */
export const Amount$inboundSchema: z.ZodType<Amount, z.ZodTypeDef, unknown> = z
  .object({
    currency: z.string(),
    value: z.number(),
  });

export function amountFromJSON(
  jsonString: string,
): SafeParseResult<Amount, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => Amount$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'Amount' from JSON`,
  );
}

/** @internal */
export const LineItem$inboundSchema: z.ZodType<
  LineItem,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  remote_id: z.string(),
  name: z.nullable(z.string()),
  amount: z.nullable(z.lazy(() => Amount$inboundSchema)).optional(),
});

export function lineItemFromJSON(
  jsonString: string,
): SafeParseResult<LineItem, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => LineItem$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'LineItem' from JSON`,
  );
}

/** @internal */
export const GetHrisPayslipsPositiveResponseResult$inboundSchema: z.ZodType<
  GetHrisPayslipsPositiveResponseResult,
  z.ZodTypeDef,
  unknown
> = z.object({
  id: z.string(),
  remote_id: z.string(),
  changed_at: z.string().datetime({ offset: true }).transform(v => new Date(v)),
  remote_deleted_at: z.nullable(
    z.string().datetime({ offset: true }).transform(v => new Date(v)),
  ),
  totals: z.lazy(() => GetHrisPayslipsPositiveResponseTotals$inboundSchema),
  employee: z.lazy(() => Employee$inboundSchema),
  pay_run: z.nullable(z.lazy(() => PayRun$inboundSchema)),
  line_items: z.array(z.lazy(() => LineItem$inboundSchema)),
});

export function getHrisPayslipsPositiveResponseResultFromJSON(
  jsonString: string,
): SafeParseResult<GetHrisPayslipsPositiveResponseResult, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      GetHrisPayslipsPositiveResponseResult$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetHrisPayslipsPositiveResponseResult' from JSON`,
  );
}

/** @internal */
export const GetHrisPayslipsPositiveResponseData$inboundSchema: z.ZodType<
  GetHrisPayslipsPositiveResponseData,
  z.ZodTypeDef,
  unknown
> = z.object({
  next: z.nullable(z.string()),
  results: z.array(
    z.lazy(() => GetHrisPayslipsPositiveResponseResult$inboundSchema),
  ),
});

export function getHrisPayslipsPositiveResponseDataFromJSON(
  jsonString: string,
): SafeParseResult<GetHrisPayslipsPositiveResponseData, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) =>
      GetHrisPayslipsPositiveResponseData$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetHrisPayslipsPositiveResponseData' from JSON`,
  );
}

/** @internal */
export const GetHrisPayslipsPositiveResponse$inboundSchema: z.ZodType<
  GetHrisPayslipsPositiveResponse,
  z.ZodTypeDef,
  unknown
> = z.object({
  status: z.literal("success"),
  data: z.lazy(() => GetHrisPayslipsPositiveResponseData$inboundSchema),
});

export function getHrisPayslipsPositiveResponseFromJSON(
  jsonString: string,
): SafeParseResult<GetHrisPayslipsPositiveResponse, SDKValidationError> {
  return safeParse(
    jsonString,
    (x) => GetHrisPayslipsPositiveResponse$inboundSchema.parse(JSON.parse(x)),
    `Failed to parse 'GetHrisPayslipsPositiveResponse' from JSON`,
  );
}
